namespace RagAgentLab.Web.Components.Shared;

/// <summary>How large the status ring is drawn.</summary>
public enum AndroidLedSize
{
    /// <summary>Inline size, for a header row.</summary>
    Small = 0,

    /// <summary>Feature size, for the top of the page.</summary>
    Large = 1,
}
